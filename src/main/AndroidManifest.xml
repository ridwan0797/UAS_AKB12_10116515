<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.school.carisma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherss"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherss"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SmakActivity" />
        <activity android:name=".SmaiActivity" />
        <activity android:name=".SwastaActivity" />
        <activity android:name=".SmkActivity" />
        <activity android:name=".SmaActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>